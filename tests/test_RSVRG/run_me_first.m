% Add folders to path.

addpath(pwd);

cd manopt;
addpath(genpath(pwd));
cd ..;

cd auxiliary;
addpath(genpath(pwd));
cd ..;

cd proposed/;
addpath(genpath(pwd));
cd ..;

cd data/;
addpath(genpath(pwd));
cd ..;


